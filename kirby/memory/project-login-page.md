# 프로젝트: 로그인 페이지 컨셉 디자인

> 생성일: 2026-03-26
> 상태: 진행 중 — A안 컨셉 확정, 디벨롭 단계

---

## 프로젝트 개요

- **목적**: 원격지원 서비스 로그인 페이지 디자인 컨셉
- **기술 스택**: Vercel + Supabase
- **컨셉**: "모든 디바이스와 사람을 연결하는 원격지원" — 추상적이고 심플한 디자인
- **산출물 위치**: `login-page/prototype/`

---

## A안 (확정 — 디벨롭 중)

### 구성 요소
- **배경**: 정적 그라데이션 `linear-gradient(135deg, #eff6ff, #f5f3ff)` (alibaba page-agent 참고)
- **컬러 오브**: `#F9E7FF`(핑크) + `#D1DFFF`(블루) 3개가 18/22/25초 주기로 페이드 인/아웃
- **노드 인터랙션**: 마우스 호버 시 노드들이 Rsupport R 로고 형태로 수렴
- **반짝임**: 별빛 트윙클 (부드러운 십자 광선, 5~10초 주기)
- **로그인 폼**: 우측 글래스모피즘 패널 (Email/PW/Google)

### R 로고 형성 시스템
- **소스 이미지**: 플랫 빨간 R 로고 (`r_logo.png`) → base64 임베드
- **감지 방식**: 빨간 픽셀 감지 (`R>150, R>G×1.3, R>B×1.3`)
- **배치 방식**: 포아송 디스크 샘플링 + 시드 고정 난수 (seed=42) → 매번 동일한 형태
- **노드 수**: 로고 300개 (idle 60개 + 호버 시 나머지 페이드인) / 배경 120개
- **노드 컬러**: `#2564ED` 계통 (HSL 220도)
- **모션**: 형성 느리게 (0.012) / 해산 빠르게 (0.035) + 중심 push

### 핵심 기술 결정 사항
1. 3D 와이어프레임 이미지 → 색상 감지 실패 (그림자/반사) → **플랫 2D 이미지로 교체하여 해결**
2. 로컬 HTML에서 이미지 로딩 → CORS 차단 → **base64 임베드로 해결**
3. 랜덤 샘플링 → 노드 불균일 → **포아송 디스크 샘플링으로 해결**
4. 매번 다른 형태 → **시드 고정 PRNG(mulberry32, seed=42)로 해결**
5. WebGL 셰이더 배경 → 색상 너무 진함 → **정적 CSS 그라데이션 + CSS 오브 레이어로 교체**

## B안 (유지)

- 다크 배경 (`#09090B`) + 블루/바이올렛/시안 소프트 오브
- 글래스모피즘 로그인 카드

---

## 다음 단계 (예상)

- A안 추가 디벨롭 (폼 디자인, 반응형, 실제 Supabase 연동)
- V0로 프로덕션 수준 정제
- B안 결정 (채택/폐기)

---

## Git 커밋 이력

| 커밋 | 내용 |
|------|------|
| `a085624` | 로그인 페이지 A/B안 컨셉 프로토타입 초기 |
| `b42e169` | 배경 개선 — 정적 그라데이션 + 컬러 오브 페이드 |
